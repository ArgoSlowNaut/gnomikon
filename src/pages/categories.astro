---
import Base from '../layouts/Base.astro';
import { categories, link } from '../lib/quotes';
---
<Base title="Topics">
  <h1>Topics</h1>
  <p class="lede">The virtues, the passions, and the life of prayer.</p>
  <ul class="list">
    {categories.map((c) => (
      <li><a href={link(`category/${c.slug}`)}><span>{c.name}</span><span class="count">{c.quotes.length}</span></a></li>
    ))}
  </ul>
</Base>
